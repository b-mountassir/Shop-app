<!DOCTYPE html>
<html>
  <head>
    <title>ShopApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="<%= controller_name %> <%= action_name %>">
    <%= render "partials/nav"%>
    <div class="container d-none">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </div>
    <div class="col-lg-12 col-md-12" id="search-results">
      <%= yield %>
    </div>  
    <%= render partial: "partials/footer"%>
      <a href="#" class="btn btn-primary back-to-top" style="display: inline;">
        <i class="fa fa-angle-double-up"></i>
      </a>
    <footer>
    </footer>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.bundle.min.js"></script>
    <%= javascript_pack_tag 'main' %>
    <%= javascript_pack_tag 'search' %>
    <%= javascript_pack_tag 'starRating' %>
    <%= javascript_pack_tag 'modalOpen', defer: true %>
    <style>
      a#customBtn:before {
        content: '-';
        background: url(
          <%= asset_path 'google_icn.png' %>
          ) no-repeat;
        background-size: 20px;
        position: absolute;
        color: transparent;
        left: 114px;
        width: 20px;
        top: 2px;
      } 
      a#customBtn {
        position: relative;
        padding-left: 30px;
      }
    </style>
  </body>
</html>
